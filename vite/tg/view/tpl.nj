<script setup lang="ts">
defineOptions({
  name: '{{ page.name }}',
})
const config = reactive(
  {{ config.props | dump | safe }}
)
const {{ page.name }} = reactive({})

</script>

<template>
  <{{ config.name or 'pro-table' }} v-bind="config" v-model="{{ page.name }}"/>
</template>

<route lang="yaml">
meta:
  title: {{ page.title or page.name}}
</route>
